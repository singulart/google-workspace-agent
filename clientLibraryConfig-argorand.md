# File downloaded from GCP Console

> This file does not contain credentials or long-lived secrets, so it does not require secret storage. However, it may include environment-specific identifiers (for example, project numbers, service account names, and identity provider paths) and should be shared only according to your organization's information-classification policy. It provides configuration hints to client libraries on how to retrieve local credentials and exchange them for short-lived Google Cloud credentials.

> Use Google Cloud Client libraries to connect to Google Cloud APIs from your application. To connect, provide a configuration file, which you can download from the JSON file below, and set an environment variable to point to it. Learn more about configuring the client library on AWS, OIDC, Azure, and X509.

> After downloading and enabling your config file, you can create policies for your workloads in IAM.


